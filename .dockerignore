references/
*.md
**/*.md
.dockerignore
.gitignore
.git
# Never bake a developer's local secret overlay into the image
assets/sample-config
assets/*.local
# Don't ship the arm64 deb in build context — only amd64 is consumed.
data/debs/*_arm64.deb
